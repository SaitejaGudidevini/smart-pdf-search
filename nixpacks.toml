[phases.setup]
nixPkgs = ["python313"]

[phases.install]
cmds = ["cd src/smart-search && pip install -r requirements.txt"]

[phases.build]
cmds = ["echo 'Build complete'"]

[start]
cmd = "cd src/smart-search && python app.py"
